import React from 'react';
import { Text, StyleSheet, View } from 'react-native';

const ListScreen = () => {

    return (
        <Text>Hello from ListScreen</Text>
    );

}
const styles = StyleSheet.create({});

export default ListScreen;