import React from 'react';
import { Text, StyleSheet, View } from 'react-native';

const ImageScreen = (props:any) => {

    return (
        <Text>Hello from ImageScreen</Text>
    );

}

const styles = StyleSheet.create({});

export default ImageScreen;