import React from 'react';
import { Text, StyleSheet, View } from 'react-native';

const TextScreen = (props:any) => {

    return (
        <Text>Hello from TextScreen</Text>
    );

}

const styles = StyleSheet.create({});

export default TextScreen;