import React from 'react';
import { Text, StyleSheet, View } from 'react-native';

const BoxScreen = (props:any) => {

    return (
        <Text>Hello from BoxScreen</Text>
    );

}

const styles = StyleSheet.create({});

export default BoxScreen;